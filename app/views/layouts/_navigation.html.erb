Navigation Links:


<p><%= link_to "All Events", events_path %></p>
<% if logged_in? %>
  <p><%= link_to "Profile", current_user %></p>
  <p><%= link_to "Create An Event", new_event_path %>
  <p><%= link_to "Logout", logout_path, method: :delete %>
<% else %>
  <p><%= link_to "Signup", new_user_path %></p>
  <p><%= link_to "Login", login_path %></p>
<% end %>